import React from "react";
import ReactDOM from "react-dom/client";
import RouterManager from "./application/RouterManager";

ReactDOM.createRoot(document.getElementById("root")!).render(<RouterManager />);
